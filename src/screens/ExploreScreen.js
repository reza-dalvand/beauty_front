import { View, Text } from "react-native";


const ExploreScreen = () => {

  return (
    <Text style={{color: '#D4AF37', textAlign: 'center', fontSize: 13, textDecorationLine: 'underline'}}>اکسپلور</Text>
  );
};

export default ExploreScreen;