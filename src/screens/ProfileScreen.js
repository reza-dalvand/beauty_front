import { Text, View } from "react-native";


const ProfileScreen = () => {

  return (
    <Text style={{color: '#D4AF37', textAlign: 'center', fontSize: 13, textDecorationLine: 'underline'}}>پروفایل</Text>
  );
};

export default ProfileScreen;